<% if @category.deleted? %>
  $("#tbl-category").DataTable().ajax.reload();
  $(".flash").html("<%= j render "layouts/flash" %>");
  <% flash.delete :success %>
<% else %>
  $(".flash").html("<%= j render "layouts/flash" %>");
  <% flash.delete :failed %>
<% end %>
